Run the test suite before completing work. When features change, update or add tests accordingly.

- When adding new configuration options, add validation logic to the corresponding `_validate_..._section` functions in `utils.py`.
- If you introduce a new placeholder (for example, for `paired_filename_template`), update both `README.md` and `config.template.yml` to document it.
